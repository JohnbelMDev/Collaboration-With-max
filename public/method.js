// To do 
    //  Find how many times each words appears
        // which words are duplicates
           //how many time did that word appears

    
let array = "Hello there where have there been a hello"
array = array.split(" ")

console.log(copyArray);
